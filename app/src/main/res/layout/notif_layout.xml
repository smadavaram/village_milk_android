<?xml version="1.0" encoding="utf-8"?>


<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/status_bar_latest_event_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white">

    <ImageView
        android:id="@+id/big_icon"
        android:layout_width="34dp"
        android:layout_height="34dp"
        android:layout_marginTop="15dp"
        android:layout_marginLeft="15dp"
        android:src="@mipmap/ic_launcher"
        android:scaleType="fitXY"
        />

    <TextView
        android:id="@+id/title"
        android:text="This is a 2 line notifications so thats why I am trying to write a lot of things here. but as you know its not that easy to do that and thats the reason I am typing all of this text."
        android:layout_width="wrap_content"
        android:layout_height="64dp"
        android:maxLines="3"
        android:textColor="@color/black"
        android:gravity="center_vertical"
        android:ellipsize="end"
        android:textSize="14dp"
        android:fadingEdge="horizontal"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:layout_toRightOf="@+id/big_icon"
        android:layout_marginLeft="15dp"/>


    <ImageView
        android:id="@+id/big_picture"
        android:layout_width="match_parent"
        android:layout_height="192dp"
        android:layout_marginTop="64dp"
        android:scaleType="fitCenter" />
</RelativeLayout>
